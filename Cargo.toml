[package]
name = "images_to_pdf"
version = "1.0.0"
edition = "2024"

[dependencies]
printpdf = { version = "0.12.5", default-features = false, features = ["png", "jpeg"] }

[profile.release]
opt-level = 3
lto = true
strip = true
